<% provide(:title, "Schedule") %>

  <div class="nine columns">
    <div class="content">
      <h1>Schedule</h1>

        <p><small>
          Please note: all classes with&nbsp;<span class="star">*</span>&nbsp;
          require pre-registration<br />
          Click the link for more information</small></p>




      <%= month_calendar events: @class_pages do |date, class_pages| %>

        <span class="day-num"><%= date.day %></span>
        <br>


        <% @recurring_classes.each do |class_page| %>
          <% if date.strftime("%A") == class_page.start_time.strftime("%A") %>
          <%= link_to class_page.class_title, class_page %>
          <br>
          <% end %>
        <% end %>



        <% class_pages.each do |class_page| %>
          <% if !class_page.recurring_event %>
            <%= class_page.class_title %>
          <% end %>
          <br>
        <% end %>

      <% end %>

      <!-- potentiel but with above ruby: need to maybe first gather all classes for the day, both
      recurring and regular class pages.
      then you gotta but it all into the same collection
      and then you gotta order them by date... it may not be a bug depending on how simple_calendar renders events
     -->





    </div>
  </div>
